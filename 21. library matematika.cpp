#include <iostream>
#include <cmath> 
using namespace std;

/*
library cmath (referensi www.cppreference.com
ceil(x)		= pembulatan keatas
cos(x)		= cosinus
exp(x)		= eksponen
fabs(x0		= nilai absolute dalam float
floor(x)	= pembulatan kebawah
fmod(x)		= modulus dalam float
log(x)		= logaritma natural
log10(x)	= logaritma berbasis 10
pow(x,y)	= x pangkat y
sin(x)		= sinus
sqrt(x)		= akar kuadrat
tan(x)		= tangen
*/

int main(){
	int a;
	cout << "menghitung akar dari : ";
	cin>>a;
	double y = sqrt(a);
	cout<<"akarnya adalah = "<< y;
	
	
}
